# Pediatric Appendicitis — Severity Benchmark : Python dependencies
# Tested with Python 3.14. Pins are the exact versions used to produce the published results;
# nearby minor versions will generally work too.

# ---- core scientific / ML stack (required) ----
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.8
joblib==1.5.3

# ---- dataset download client ----
ucimlrepo>=0.0.7

# ---- models benchmarked ----
xgboost==3.3.0
catboost==1.2.10
imbalanced-learn==0.14.2      # provides BalancedRandomForestClassifier (import name: imblearn)
interpret==0.7.8              # provides the Explainable Boosting Machine (EBM)

# ---- Jupyter: to open / run / re-execute the notebook ----
jupyterlab>=4.0
ipykernel==7.3.0
nbformat==5.10.4
nbclient==0.11.0              # optional: headless execution of the notebook

# ---- OPTIONAL: TabPFN v2 (tabular foundation model) ----
# Gated weights: requires a one-time, free licence acceptance (interactive `import tabpfn`
# login, or set the TABPFN_TOKEN environment variable). Pulls PyTorch.
# tabpfn==8.0.8
# torch==2.11.0
